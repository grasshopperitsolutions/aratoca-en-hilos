## Brief overview

This file defines file patterns that Cline should ignore when processing the project. Files matching these patterns will be excluded from context, analysis, and tool operations.

## Ignore patterns

- `*-ignore*` — Files with "-ignore" in their name (e.g., `aratoca_en_hilos-ignore.tsx`)