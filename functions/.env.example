# From address for outbound mail. Must be on a domain verified with the email
# provider, or every send is rejected.
#
# Copy to functions/.env and set the real value. The API key is NOT here — it is
# a secret, set with:  firebase functions:secrets:set RESEND_API_KEY
EMAIL_FROM=Aratoca en Hilos <no-responder@grasshoppersolutions.online>